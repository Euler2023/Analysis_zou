# 编译与审计报告

## 当前结果

- 主文件：`main_driver.tex`；模板：Qbook；引擎：XeLaTeX.
- 最新完整构建：成功，生成 `main_driver.pdf`，A4，共 592 页.
- 输入模块：前置内容、10 章正文、符号索引、名词索引.
- 静态结构检查：通过.
- 基础 `check_fidelity.py`：通过环境配对扫描，但扫描到 0 个 label 与 0 个 citation，不能替代源文忠实性审阅.
- 严格源文忠实性：**未通过**，详见 `work/fidelity-report.md` 及四份分章报告.
- 公式/盒子溢出：**通过**；超过 5pt 的项目为 0，详见 `work/math-overflow-report.md`.
- 编译日志的缺字告警为 0；此前第 3 章页眉的 6 条字体告警也随标题计数结构规范化而消失。
- PDF 链接：原扫描 PDF 为 0 个内部链接；成品具有内部链接，但结果只能记为不可比较.
- source manifest 已改为列出实际参与转换的 13 个 `work/*.normalized.md`，并把对应的 13 个原始 OCR Markdown 单独列出；`source_metadata.md` 不计入正文源.
- 习题边界专项审计：全书现有 41 对配平的 `exercise`；删除 55 对由题干命令词“证明”误生成的 `proof`，习题内部嵌套 `proof` 数为 0。正文中 200 个真正的证明环境保留。
- 第 1 章 §2 习题已按原扫描页恢复为例 12 后起、`§3 映射` 前止；第 9 章章末习题已补齐统一外框。
- 本轮正文修订均直接写入 `tex/chapter01.tex`--`chapter10.tex`；没有运行 Markdown→TeX 转换器，也没有用 normalized Markdown 覆盖人工修订。
- 章号已改为由 LaTeX 的 `chapter` 计数器自动映射：各章源码只保留 `\chapter{章标题}`，Qbook 模板统一生成“第1章”至“第10章”；目录与 PDF 书签不再出现 `Chapter 1 第1章` 这类中英文重复编号。
- 节号与小节号也已交由 LaTeX 自动映射：正文源码不再手写 `§1` 或 `1.`，模板分别生成 `§1`、`1.` 等源书格式；“习题”、`1)` 型内部分类和无编号过渡标题使用星号命令，不占用计数器。目录与 PDF 书签已同步消除 `10.1 §1`、`10.1.1 1.` 这类重复编号。
- 编号清理同时恢复了第3章 §4 的“6. 有界序列的收敛子序列”、第4章 §4 的“1. 函数的上、下确界”，并将第9章 §1 的三类广义积分标题恢复为连续的小节结构。
- 已逐页重录或修正：第 1 章习题、第 2 章实数构造、第 3 章极限定义，第 4 章不定型/单调间断，第 5 章开篇与双曲函数，第 6 章 Rolle/Darboux/L'Hospital，第 7 章相关习题，第 8 章末习题，第 9 章第三类广义积分，以及第 10 章球极投影、§3 习题与例 8--10。

## 本轮构建命令

```bash
latexmk -xelatex -halt-on-error -interaction=nonstopmode -g \
  -auxdir=build -outdir=. main_driver.tex
```

## 交付状态

当前成品已通过编译、基础 fidelity、静态结构和失败级溢出检查。尚未逐页覆盖的 OCR 区间仍列在分章报告中，因此严格逐页学术忠实性保持未通过，不能把“自动检查通过”解释为全书内容校勘完成。
